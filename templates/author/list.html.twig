<!DOCTYPE html>
<html>
<head>
    <title>Authors List</title>
</head>
<body>
    <h1>List of Authors</h1>

    {% if authors is empty %}
        <p>No authors found.</p>
    {% else %}
        <p>Total Authors: {{ authors | length }}</p>
        <p>Total Books: {{ authors | reduce((carry, author) => carry + author.nb_books, 0) }}</p>
        
        <ul>
            {% for author in authors %}
                <li>
                    <img src="{{ author.picture }}" alt="{{ author.username }}" width="50" height="50">
                    {{ author.username | upper }} - 
                    <a href="{{ path('author_details', { id: author.id }) }}">Details</a>
                </li>
            {% endfor %}
        </ul>
    {% endif %}
</body>
</html>
